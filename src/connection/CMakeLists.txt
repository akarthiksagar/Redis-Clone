add_library(
    connection
    Connection.cpp
)
target_include_directories(
    connection
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
)
target_link_libraries(
    connection
    PUBLIC
    request
)